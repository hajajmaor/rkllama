# Full Install & Test — Actions and Results

Date: 2026-01-21

Purpose: perform the full "do everything" flow: install build deps, build and install `rkllm` runtime from `ezrknn-llm`, run the runtime on `gemma-3n-E4B-it`, and record outcomes for comparison.

Environment
- Workspace: `/workspaces/rkllama` (branch: `maor-dev`).
- Host arch: `aarch64` (ARM).
- Python: 3.12.12.

Steps executed (commands and results)

1) Install build dependencies
- Command:
  apt-get update && apt-get install -y cmake build-essential pkg-config libssl-dev
- Outcome: installed `cmake`, `gcc`, `g++`, `make`, `pkg-config`, `libssl-dev`.

2) Re-run `ezrknn-llm/install.sh` to build runtime
- Command:
  (cd ezrknn-llm && bash install.sh) > /tmp/ezrknn_install2.log 2>&1
- Outcome: CMake configured and compiled `llm_demo` successfully. Build log in `/tmp/ezrknn_install2.log` shows
  - "Built target llm_demo"
- The script copied the built binary to `/usr/bin/rkllm`.

3) Verify installed binary
- Command: `ls -l /usr/bin/rkllm` and `/usr/bin/rkllm --help`
- Outcome: `/usr/bin/rkllm` exists and reports usage: `Usage: /usr/bin/rkllm model_path max_new_tokens max_context_len`.

4) Run the `rkllm` binary against the Gemma model (short run)
- Command: `/usr/bin/rkllm /workspaces/rkllama/models/gemma-3n-E4B-it/gemma-3n-E4B-it-rk3588-w8a8-opt-0-hybrid-ratio-0.0.rkllm 32 4096`
- Outcome: binary started but exited with error:
  - "I rkllm: rkllm-runtime version: 1.2.1, rknpu driver version: 0.9.8, platform: RK3588"
  - "E rkllm: unsupported model architecture: gemma3n"
  - "rkllm init failed"

Interpretation and notes
- Build succeeded and runtime binary is available. Good.
- The built runtime reports version `1.2.1` while earlier logs (from other installs/runs) showed `1.2.3`; model files in the workspace were apparently created for a newer runtime and include a `gemma3n` architecture not supported by the built `1.2.1` runtime.
- The remaining blocker for running the `gemma-3n-E4B-it` model is runtime/toolkit compatibility: either use a runtime that supports `gemma3n` (e.g., rkllm-runtime v1.2.3+), or use a model converted/compiled for the installed runtime.

Files & logs produced
- `/tmp/ezrknn_install2.log` — build and install log.
- `/usr/bin/rkllm` — installed runtime binary.
- `/tmp/rkllm_run.txt` — the runtime invocation output showing the unsupported architecture error.
- `documentation/current_state.md` — baseline before this full run.
- `documentation/rkllm_toolkit_install_experiment.md` — wheel-install experiment notes.
- `documentation/ezrknn_install_results.md` — earlier partial run notes.
- `documentation/install_and_test_results.md` — this summary file.

Recommendations / next steps
1. To run `gemma-3n-E4B-it` successfully on this host:
   - Install/use an `rkllm-runtime` binary that supports `gemma3n` (v1.2.3 or newer). Options:
     - Obtain a prebuilt rkllm runtime (shared library + binary) matching the model (from rkllm-toolkit builds or releases) and place it under `/usr/lib` and `/usr/bin` as appropriate.
     - Rebuild the runtime from source using a branch/tag that includes `gemma3n` support (if available in upstream sources) and re-run the install.
2. If using the Python `rkllm_toolkit` SDK is desired, obtain an aarch64 wheel or build the toolkit from source on this host.
3. If allocation errors reappear, capture `dmesg -T | tail -n 200` after a failed run to inspect kernel driver / CMA errors.

Comparison plan
- Use `documentation/current_state.md` as the baseline (pre-build). After making the runtime/toolkit versions aligned (or using a matching model), re-run the same steps and compare these files plus runtime outputs and kernel logs.


Generated by the automated experiment runner on 2026-01-21.
